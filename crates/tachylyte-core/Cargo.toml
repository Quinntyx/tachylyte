[package]
name = "tachylyte-core"
version = "0.1.0"
edition = "2021"
description = "Local-first vault primitives for Tachylyte"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
